#!/usr/bin/env bash

SCRIPTPATH="$( cd "$(dirname "$0")" ; pwd -P )"
cd $SCRIPTPATH

# delete the files generated by TeX
cd ../docs
rm -vf *log
rm -vf *aux
rm -vf *out

# delete .pyc files
cd ../src/lib
rm -vf *pyc

cd ply
rm -vf *pyc

# delete PLY stuff
rm -vf ../parsetab.py
